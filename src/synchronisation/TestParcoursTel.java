package synchronisation;

import java.io.File;
import java.io.IOException;
import java.nio.file.DirectoryIteratorException;
import java.nio.file.DirectoryStream;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;

import org.omg.CORBA.Environment;

public class TestParcoursTel {

	public static void main(String[] args) {
		
		
//		Path dir = Paths.get("Ordinateur/Benjamin Michau_G3/Carte SD/music");
//		try (DirectoryStream<Path> stream = Files.newDirectoryStream(dir)) {
//		    for (Path file: stream) {
//		        System.out.println(file.getFileName());
//		    }
//		} catch (IOException | DirectoryIteratorException x) {
//		    // IOException can never be thrown by the iteration.
//		    // In this snippet, it can only be thrown by newDirectoryStream.
//		    System.err.println(x);
//		}
//		File myFile = new File("/sdcard/" + txtName.getText() + ".txt");
//		File myFile = new File(Environment.getExternalStorageDirectory().getAbsolutePath() + "/"+ txtName.getText() + ".txt");
//		System.out.println("jjd");
		

	}

}
